<%= render "govuk_publishing_components/components/select", {
  id: "topic-select",
  name: "primary_topic",
  label: "Topic",
  data_module: { module: "topics-select" },
  options: primary_topics_options(filter_params[:primary_topic])
} %>

<%= render "govuk_publishing_components/components/select", {
  id: "topic-select-target",
  name: "secondary_topic",
  label: "Sub-topic",
  disabled: filter_params[:primary_topic].blank?,
  options: secondary_topics_options(filter_params[:primary_topic], filter_params[:secondary_topic])
} %>
