<%= render "govuk_publishing_components/components/select", {
  id: "topic-select",
  name: "level_one_taxon",
  label: "Topic",
  data_module: { module: "topics-select" },
  options: primary_topics_options(params[:level_one_taxon])
} %>

<%= render "govuk_publishing_components/components/select", {
  id: "topic-select-target",
  name: "level_two_taxon",
  label: "Sub-topic",
  disabled: params[:level_one_taxon].blank?,
  options: secondary_topics_options(params[:level_one_taxon], params[:level_two_taxon])
} %>
